context.properties = {
    default.clock.rate = 48000
    default.clock.quantum = 4096          # Bump to this if not already
    default.clock.min-quantum = 2048      # Raise min too to prevent dropping lower
    default.clock.max-quantum = 8192      # Allow up to this
    default.clock.quantum-limit = 16384   # Optional: higher ceiling
}
