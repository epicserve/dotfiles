[core]
  autocrlf = input

[color]
  diff = auto
  status = auto
  branch = auto

[pull]
	rebase = true

[gpg]
  format = ssh

[commit]
  gpgsign = true

[credential]
  helper = store

[include]
  path = ~/.config/git/config_local
